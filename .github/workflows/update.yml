name: Update Rankings

on:
  schedule:
    - cron: "0 */1 * * *"   # a cada 1 hora
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Prepare folders
        run: |
          mkdir -p data
          mkdir -p docs/data

      - name: Fetch downloads
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/fetch_downloads.js

      - name: Validate generated data
        run: |
          node --check scripts/fetch_downloads.js
          node --check scripts/generate_readme.js
          node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('data/rankings.json','utf8')); if (!Array.isArray(p.results) || !p.results.length) throw new Error('Ranking vazio'); if (!fs.existsSync('data/history.json')) throw new Error('Historico ausente');"

      - name: Generate README
        run: node scripts/generate_readme.js

      - name: Move rankings to docs
        run: |
          if [ -f data/rankings.json ]; then
            mv data/rankings.json docs/data/rankings.json
          fi
          if [ -f data/history.json ]; then
            mv data/history.json docs/data/history.json
          fi

      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add README.md docs/data/rankings.json docs/data/history.json

          git commit -m "Update rankings (auto)" || exit 0

          git push
